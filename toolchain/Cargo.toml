[package]
name = "playground-toolchain"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# Pulls in everything a guest program links against, so that one `cargo build` produces the
# whole rlib set for the browser sysroot.
[dependencies]
egui.workspace = true
egui_playground.workspace = true
pg-abi.workspace = true
